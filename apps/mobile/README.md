# React Native + Expo (planned)
The mobile app will use a native-capable Expo development build for iBeacon monitoring and active ranging. Manual room lookup uses the same API and must work without BLE. Test real iOS/Android background restrictions before shipping automatic alerts.
