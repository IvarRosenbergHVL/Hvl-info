# React admin (planned)
Entra ID authenticated admin for places, devices, manuals, incidents and publications. The first API endpoints live in `apps/api`. Do not treat this placeholder as a working UI.
