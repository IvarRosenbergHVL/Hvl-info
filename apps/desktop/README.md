# Tauri + React (planned)
Windows/macOS BLE, Entra ID native login and a compact context-aware assistant UI will use the same API. Native BLE and OS permission behavior need a proof-of-concept before committing to a plugin.
